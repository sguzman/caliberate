# Architect review — task 0010.3

Status: **REJECTED — one bounded correction required**

Reviewed implementation commit:

- `ae45595983435b2637be1d2c71f80cb872637cae`

The overall approach is accepted:

- Calibre-specific `Title - Author` parsing stays in the development bootstrap;
- last separator is used;
- generic Rust metadata parsing is untouched;
- dedicated dev DB is correctly isolated at `.cache/caliberate/data/en-nonfiction-dev.sqlite`;
- `-Reset` is scoped to that DB and its WAL/SHM sidecars;
- root wrapper delegates to the canonical script;
- source library is not mutated.

## Required correction — multi-author CLI invocation

The parser can return multiple authors, but the live command currently invokes:

```powershell
& $calibredb --config $DevConfigPath set authors --id $addedId --value $metadata.Authors
```

The CLI shape is:

```rust
Authors {
    #[arg(long)]
    id: i64,
    #[arg(long)]
    value: Vec<String>,
}
```

Do not rely on one `--value` followed by a PowerShell array expanding into multiple positional native arguments. Construct the invocation so each author is explicitly represented using the CLI's supported repeated-option form, e.g. conceptually:

```text
set authors --id N --value "Jane Doe" --value "John Roe"
```

Use a small helper to build the author argument vector if that keeps the script clear.

Add a PowerShell self-test that verifies the generated argument list for multiple authors contains repeated `--value` flags in deterministic order.

Keep all other behavior unchanged.

## Revalidation

Rerun:

```text
pwsh -NoProfile -File .\scripts\setup-en-nonfiction-dev.ps1 -SelfTest
pwsh -NoProfile -File .\setup-en-nonfiction-dev.ps1 -SelfTest
cargo fmt --check
cargo check --workspace --locked
cargo test --workspace --locked
```

Update `docs/work/reports/0010.3.md`, push the same branch, and return the checkout to `main`.

Do not touch task 0011 or the source Calibre library.
