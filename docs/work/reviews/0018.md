# Architect review — task 0018, correction 2

Status: **REJECTED — two tiny test/report evidence gaps remain**

Reviewed correction commit:

- `f8401ec3eb5619ca4184331e6707624db5caf512`

Production code remains accepted in shape. Do not change it unless one of the two assertions below reveals a concrete defect.

## What is now accepted

The correction now genuinely exercises attached-Calibre JSON behavior through:

```rust
ServerState::with_attached_calibre(...)
```

and proves:

- attached JSON browse/query;
- two-format detail and format listing;
- deterministic PDF -> EPUB `data.id` order;
- no obvious metadata/source-path fields in JSON;
- EPUB/PDF raw content streaming;
- case-insensitive format request;
- unavailable-format 404;
- primary lowest-`data.id` content compatibility;
- attached content streaming with `download_allow_external=false`;
- unchanged attached `metadata.db` bytes;
- configured simple-search paging;
- browse sort/paging;
- two facet kinds;
- configured external-reference denial;
- bearer-auth protection;
- prefixed generated `content_href`.

Keep all of this.

## Gap 1 — attached source-isolation claim is not actually proved

The report currently says:

> It verifies attached-source selection against a distinct configured source

But `attached_state()` loads the normal repository config and does not assign:

```rust
config.db.sqlite_path = <distinct synthetic must-not-open or differently seeded db>
```

So the current test proves attached data is returned, but it does not strongly prove the API could not have accidentally opened the configured source.

### Required fix

Inside the attached fixture/state setup, point:

```rust
config.db.sqlite_path
```

at a distinct synthetic path that must not be used by the attached JSON handlers.

Preferred simple form:

```text
<temp>/must-not-open.db
```

and assert after representative attached JSON requests that this configured DB path was not created/opened.

Alternatively seed it with obviously different content and assert none of that content appears, but the must-not-open path is cleaner.

This must remain entirely synthetic.

Then the report may accurately retain the source-isolation claim.

## Gap 2 — JSON error Content-Type is not asserted

The review required:

- successful metadata endpoint -> JSON Content-Type;
- JSON error endpoint -> JSON Content-Type.

The correction currently asserts only the successful `/api/v1/books` response header.

### Required fix

Add one direct response assertion for an API JSON error, for example:

```text
GET /api/v1/books?limit=0
```

Assert:

- status 400;
- `Content-Type` starts with `application/json`;
- optionally retain the existing `invalid_request` body assertion.

Do not change production code unless this fails.

## Report

Update `docs/work/reports/0018.md` only as needed to match the final evidence exactly.

## Scope

Test/report correction only.

Do not:

- add routes;
- redesign DTOs;
- change OPDS;
- change content authorization;
- touch GUI;
- access the real Calibre library;
- work on any other task.

Preserve local `config/control-plane.toml`.

## Validation

Rerun:

```text
cargo fmt --check
cargo test -p caliberate-library
cargo test -p caliberate-server
cargo test -p caliberate-app --bin calibre-server
cargo check --workspace --locked
cargo test --workspace --locked
```

Push the same branch:

```text
codex/0018-http-json-library-api
```

and return the checkout to `main`.
