# Architect review — task 0019

Status: **REJECTED — tiny test/report correction required**

Reviewed implementation commit:

- `80b64746fa64d6b6c7cbff4f213488fe9bc5fd09`

## Production assessment

The production implementation is accepted in shape and should remain unchanged unless the tests below expose a concrete defect:

- legacy `/opds/books/{id}/download` remains the primary compatibility route;
- `/opds/books/{id}/download/{format}` is additive;
- alternate links are sourced from `LibraryCatalog::list_formats(id)`;
- the primary format is excluded from alternate links;
- alternate links retain service order;
- format-specific resolution uses `LibraryCatalog::resolve_content_format`;
- primary and format-specific downloads share `content::stream_content`;
- JSON API is untouched;
- URL prefix and auth integration are present.

## Remaining evidence gap 1 — configured unavailable alternate route is not tested

The task required configured-Database compatibility coverage proving:

> an unavailable alternate format returns 404

The report claims:

> Configured-Database tests prove ... unavailable alternate behavior

but the current configured-Database OPDS test never calls the new route.

### Required fix

In the existing configured single-format fixture, request an unavailable alternate such as:

```text
GET /opds/books/{id}/download/pdf
```

when the canonical format is EPUB, and assert:

```text
404 Not Found
```

Also request the canonical format-specific route:

```text
GET /opds/books/{id}/download/epub
```

and assert it succeeds and returns the same bytes as the legacy primary route.

This confirms both the new route and the no-duplicate-link compatibility model.

Do not change production code unless this exposes a real defect.

## Remaining evidence gap 2 — alternate ordering claim is only vacuously tested

The attached fixture currently has:

- PDF primary;
- EPUB alternate.

With only one alternate, the test cannot actually prove that multiple alternate links preserve `LibraryCatalog::list_formats` order, although the production loop appears correct.

### Required fix

Add a third synthetic format row after EPUB, for example:

```text
data.id 12 = MOBI
```

with a small distinct synthetic file.

Then assert the book-entry XML places:

```text
/download/epub
```

before:

```text
/download/mobi
```

after the legacy primary link.

You do not need to add extensive MOBI streaming coverage; one ordering assertion is sufficient. Existing EPUB/PDF byte tests should remain.

## Report

Update `docs/work/reports/0019.md` so the configured unavailable-alt and multi-alternate ordering claims are backed by the actual tests.

## Scope

Test/report correction only.

Do not:

- redesign OPDS;
- change JSON;
- change library-domain semantics;
- add routes;
- touch GUI;
- access the user's real Calibre library;
- work on another task.

Preserve local:

```text
config/control-plane.toml
```

## Validation

Rerun on native Windows:

```text
cargo fmt --check
cargo test -p caliberate-library
cargo test -p caliberate-server
cargo test -p caliberate-app --bin calibre-server
cargo check --workspace --locked
cargo test --workspace --locked
```

Push the same branch:

```text
codex/0019-opds-multi-format
```

and return the checkout to `main`.
