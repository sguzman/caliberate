# 0006 — Route OPDS download through library content locator

## Summary of changes

- Updated `opds_book_download` to resolve book content through `LibraryCatalog::resolve_content`.
- Preserved the existing server-side authorization, path policy, size checks, metadata handling, streaming, MIME mapping, content length, routes, and status behavior.
- Scoped the catalog borrow to the resolver call so the owned `LibraryContent` can safely be used across async filesystem operations.

## Files changed

- `crates/server/src/opds.rs`
- `docs/work/done/0006-opds-download-use-content-locator.md`
- `docs/work/reports/0006.md`

## Validation

All required commands passed on native Windows:

- `cargo fmt --check` — passed.
- `cargo test -p caliberate-server` — passed; 6 tests passed.
- `cargo check --workspace --locked` — passed.
- `cargo test --workspace --locked` — passed; all workspace tests and doctests passed.

The workspace validation emitted the repository's existing `caliberate-gui` warnings; they did not fail validation and were outside this task's scope.

## Risks or unverified behavior

- The OPDS download path was validated by the existing server integration tests on Windows.
- No separate Linux runtime validation was performed in this task.

## Deviations

None. No dependency, lockfile, database, schema, GUI, or unrelated protocol changes were made.
