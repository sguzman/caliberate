# 0008 — Database-backed library sorting, pagination, and totals

## Summary

- Added database query sort fields, deterministic ordering, offset pagination, and filtered total counting.
- Shared structured-filter construction between the DB result and count queries; no new SQL schema or dependency was added.
- Added library-domain sort fields and `LibraryQueryPage`, with `LibraryCatalog::query_page` returning page metadata and full filtered totals.
- Added focused DB and library tests for sorting, tie-breaking, pagination, distinct relation counts, and page metadata.

## Files changed

- `crates/db/src/query/mod.rs`
- `crates/db/src/database.rs`
- `crates/db/tests/query.rs`
- `crates/library/src/query.rs`
- `crates/library/src/catalog.rs`
- `docs/work/done/0008-library-query-pages.md`
- `docs/work/reports/0008.md`

## Validation

All required commands passed on native Windows:

- `cargo fmt --check` — passed.
- `cargo test -p caliberate-db --test query` — passed; 9 tests passed.
- `cargo test -p caliberate-library` — passed; 10 library unit tests, 4 ingest tests, and doctests passed.
- `cargo check --workspace --locked` — passed.
- `cargo test --workspace --locked` — passed; all workspace tests and doctests passed.

The workspace validation emitted the repository's existing `caliberate-gui` warnings; they were not changed by this task.

## Risks and unverified behavior

- Sorting, pagination, and count behavior were tested against temporary SQLite databases and the full native Windows workspace suite.
- No separate Linux validation was performed in this task.

## Deviations and blockers

None. No GUI, server, OPDS, HTTP/JSON, schema, migration, dependency, caching, or unrelated library changes were made.
