# Work report: 0010.1 GUI pane layout ergonomics

## Summary

Refactored the library shell so the Library table/grid/shelf is rendered in the
central area. Browser and Details are independently rendered as resizable left or
right auxiliary panels with compact header collapse controls. Existing visibility,
docking-side, layout preset, selection, details, browser, jobs, and persistence
behavior remain connected to the existing application flow.

## Layout and width behavior

- Auxiliary panes use a 200 px minimum and 720 px hard maximum.
- The central library reserves 480 px whenever the available window width permits.
- `clamp_auxiliary_pane_widths` deterministically shrinks requested widths toward
  the auxiliary minimum when persisted values would starve the central surface.
- `auxiliary_pane_max_width` limits each active separator against the other active
  pane and the central minimum, while still allowing useful dragging.
- `left_pane_width` and `right_pane_width` now represent auxiliary pane widths on
  the configured left and right sides; the library no longer consumes either width.

The left/right fields remain preferred widths. Runtime effective widths are derived
from the current window and are not written back when the runtime maximum is below
the preferred width. A response width is recorded only when the preferred width
fits the current runtime maximum, which preserves genuine user resizes in a wide
window while preventing narrow-window clamping from replacing the preference. The
existing application layout save path persists recorded preferred widths, and
restoring available space reuses the original preference.

`CaliberateApp::pull_layout_from_library` now compares Browser/Details visibility,
docking sides, Jobs visibility, and both preferred widths before copying state. Any
change marks `shell_config_dirty`, so direct pane collapse is persisted through the
existing shell config save path.

Details fields including path, title, authors, tags, identifiers, and other visible
metadata use wrapping labels with hover text preserving the full value.

## Files changed

- `crates/gui/src/views.rs`
- `docs/work/ready/0010.1-gui-pane-layout-ergonomics.md` moved to
  `docs/work/done/0010.1-gui-pane-layout-ergonomics.md`
- `docs/work/reports/0010.1.md`

The pre-existing user modification to `config/control-plane.toml` was preserved
and was not staged or changed.

## Validation

Passed on native Windows:

- `cargo fmt --check`
- `cargo test -p caliberate-gui` — 30 unit tests, GUI smoke integration test, and doctests passed
- `cargo check --workspace --locked`
- `cargo test --workspace --locked`

The existing GUI warning backlog remains (57 warnings, including deprecated egui
APIs and unused/dead code).

## Interactive verification and deviations

Interactive GUI behavior was not verified by the worker because this session does
not expose the required desktop-control surface. No interactive runtime behavior
is claimed as verified. The human verification steps in the task remain required
after integration.

No dependencies, query/service semantics, schemas, persistence formats, or
unrelated task behavior were changed. The architect review corrections were
limited to preferred-width persistence and complete shell-state dirty detection.
