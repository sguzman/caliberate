# Core Roadmap

- [x] Define baseline crate structure and module boundaries
- [x] Define config schema contracts and defaults
- [x] Implement config loader with validation and error reporting
- [x] Implement tracing bootstrap with structured fields and spans
- [x] Define global error types and error handling policy
- [ ] Establish app lifecycle (startup, shutdown, signal handling)
- [ ] Add concurrency/runtime configuration wiring
- [ ] Add metrics stubs and interfaces (no-op initially)
