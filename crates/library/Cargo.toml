[package]
name = "caliberate-library"
version = "0.1.0"
edition = "2024"

[dependencies]
caliberate-core = { path = "../core" }
caliberate-assets = { path = "../assets" }
caliberate-metadata = { path = "../metadata" }
crossbeam-channel = "0.5.15"
tracing = "0.1.44"

[dev-dependencies]
tempfile = "3.27.0"
